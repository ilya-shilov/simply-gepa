# GEPA LLM Connection Settings
# These map directly to Settings fields: api_key, model, base_url, temperature.
# pydantic-settings reads them case-insensitively (API_KEY -> api_key).

API_KEY=your-api-key-here
MODEL=gpt-4o-mini
TEMPERATURE=0.7

# For local models (SGLang, vLLM, Ollama) — set base_url, no API key needed:
# BASE_URL=http://localhost:8000/v1
# MODEL=default

# The CLI also checks these env vars for API key (in priority order):
# GEPA_API_KEY=...
# OPENAI_API_KEY=...

# Profile can be set via env var (used by CLI):
# GEPA_PROFILE=balanced
